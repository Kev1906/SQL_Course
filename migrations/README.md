# Database Migrations

This directory is reserved for tracking schema updates, migrations, and structural modifications as the DataMartX database grows.

## Structure
* Use standard chronological ordering (e.g., `V1__init.sql`, `V2__add_analytics.sql`) for any migration tool (like Flyway, Liquibase, or dbmate) introduced during the course.
